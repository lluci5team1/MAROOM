export type Product = {
  id: string; // UUID → string in TypeScript
  title: string;
  category: string;
  brand: string;
  style: string;
  color: string;
  price: number; // Integer → number
  roomType: string;
  productUrl: string;
};
